import 'package:flutter/material.dart';

class GestionPresence extends StatefulWidget {
  const GestionPresence({super.key});

  @override
  State<GestionPresence> createState() => _GestionPresenceState();
}

class _GestionPresenceState extends State<GestionPresence> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
